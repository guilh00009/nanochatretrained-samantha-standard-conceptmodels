# Use PyTorch CUDA 12.1 wheels on Windows
--index-url https://download.pytorch.org/whl/cu121
--extra-index-url https://pypi.org/simple

# Core deps (exact pins you gave)
torch==2.8.0
gradio==5.49.1
tiktoken==0.12.0
numpy==2.2.6
huggingface_hub==0.35.3

# Helpful runtime for HF models (safe and tiny)
safetensors>=0.4.5

# Visualization for concept mapper
matplotlib>=3.9.0

# Word list sampling for concept sweeps
wordfreq>=3.0.0
